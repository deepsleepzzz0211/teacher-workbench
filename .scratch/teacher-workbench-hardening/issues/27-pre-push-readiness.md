# 27: 推送远端前的就绪性修复

**What to build:** 推送前检查发现四处需要在首次推送前处理的问题：环境变量忽略规则有缺口、演示凭据需要显式警示、开发期依赖带漏洞且其中的迁移生成工具有冲突隐患、缺少许可证。逐项修掉，让仓库可以安全推送。

**Blocked by:** None（可立即开始）

**Status:** done

- [x] 环境变量忽略规则覆盖所有变体——改为 `.env` + `.env.*` + `!.env.example`；实测 `.env` / `.env.production` / `.env.test` / `.env.local` / `apps/api/.env` 全部被忽略，而 `.env.example` 保留
- [x] README 顶部显式警示：数据库口令、演示账号口令、JWT 开发默认值均为公开固定值，并写明部署前必须更换
- [x] 移除迁移生成工具（`drizzle-kit`）：其传递依赖链上的 `esbuild` 漏洞随之消失，同时消除了"生成式迁移与手写 SQL 唯一事实来源相冲突"的隐患
- [x] 补上许可证（MIT）
- [x] 全量测试与类型检查保持绿色；依赖漏洞经审计确认下降

## 验证方式

**移除依赖后，迁移路径必须自证独立**——这是本次最需要确认的一点：

| 检查 | 结果 |
|---|---|
| `pnpm db:migrate` | 「数据库结构已是最新」——正常，证明迁移器不依赖被移除的工具 |
| `pnpm db:reset` | 重建 schema + 应用 1 个迁移 + 灌入演示数据，正常 |
| 依赖漏洞 | **3 moderate → 2 moderate**（剩余两项为同一条 vitest 公告）；`esbuild` 整条链路消失，依赖树减少 163 个包 |
| `pnpm typecheck` | exit 0 |
| 单元/集成/组件 | **276 条全通过** |
| 端到端 | **26/26 全通过** |

## 说明

- **许可证类型是我替你选的**：MIT，版权署名取自仓库的提交身份。若要换成 Apache-2.0 或私有，改 `LICENSE` 与 README 结尾一行即可。
- **剩余 2 个 moderate 未处理**：`vitest` 的路径遍历公告，修复需跨大版本升级（3 → 4）。它属开发期依赖，且触发前提是攻击者已能控制测试文件。推送前做大版本升级的风险与收益不成比例，留待推送后单独处理。
- **安全响应头未加**：已在 README 的"已知边界"里如实列为未做项，避免读者误以为已覆盖。
