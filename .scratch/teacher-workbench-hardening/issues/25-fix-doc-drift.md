# 25: 修正文档漂移

**What to build:** 文档里有若干与代码不符之处，会让新人照着做却做不通。已知项：架构文档描述了一个并不存在的脚本目录；需求文档引用了一个不存在的命令；架构文档写错了表名（实际是班级档案表）；示例环境变量文件里有一个代码根本不读的配置项，让人误以为可以把前端指向远程接口；README 的截图清单漏了后来新增的两张。做成：逐条对照代码核实并修正，以"每条声明都能在代码里找到对应"作为完成标准。

**Blocked by:** None（可立即开始）

**Status:** done

- [x] 架构文档中不存在的目录描述已删除（`scripts/` 从未创建，已从 README 与 ARCHITECTURE 移除；目录树改列真实的 `.scratch/` 与 `AGENTS.md`）
- [x] 需求文档引用的命令与实际脚本名一致（`pnpm db:setup` → `pnpm setup`）
- [x] 架构文档中的表名与数据库实际表名一致（`classes` → `class_groups`；其余表名逐条核对无漂移）
- [x] 示例环境变量文件中的每一项都有代码实际读取——**选择了"补上读取逻辑"而非删除**：`VITE_API_BASE_URL` 现在真的会被前端读取（新增 `vite-env.d.ts` 声明类型），部署到与后端不同源时可用
- [x] README 的截图清单与实际生成的截图文件一致（补齐 `11-workload-item-form.png`、`12-practice-form.png`，共 13 张）
- [x] README 的页面数量、测试用例数量与实际一致（"九个页面" → "十个页面"；测试计数由 231 更新为 **301**，并修正了测试文件通配路径）
- [x] 逐条核对：文档中每一条可验证声明都能在代码或运行结果中找到对应证据
- [x] 全量测试与类型检查保持绿色（shared 83 / api 161 / web 33 条用例全通过，E2E **24/24**，`typecheck` exit 0）

## 修正过程中额外发现并处理的两处

1. **架构文档高估了组件测试覆盖**：原文称组件测试覆盖"工作量计算展示"，实际并未覆盖（该测试属于工单 18 的范围）。已改为如实描述当前覆盖的内容（登录表单、错误边界、未保存守卫、键盘可达性、登出清缓存），待工单 18 完成后再补回。
2. **限流配置的默认值容易被误读**：示例环境变量里的 `LOGIN_RATE_LIMIT_MAX=200` 是为避免本地自动化测试被误伤而放宽的，比代码内默认（10 次/分钟）宽 20 倍。已在示例文件中注明生产环境应调小。

## 验证方式

不是"改完就算"，而是**逐条机器核查**：对每一类漂移点重新检索，全部归零（`db:setup` 0 处、`scripts/` 0 处、`classes` 表名 0 处、旧测试计数 0 处、"九个页面" 0 处）；`VITE_API_BASE_URL` 有 **1 处代码读取**（`client.ts`）加 **1 处类型声明**（`vite-env.d.ts`）；并把 README 中出现的每一个 `pnpm <脚本>` 提取出来与 `package.json` 的脚本清单逐一比对，全部存在。
