# 25: 修正文档漂移

**What to build:** 文档里有若干与代码不符之处，会让新人照着做却做不通。已知项：架构文档描述了一个并不存在的脚本目录；需求文档引用了一个不存在的命令；架构文档写错了表名（实际是班级档案表）；示例环境变量文件里有一个代码根本不读的配置项，让人误以为可以把前端指向远程接口；README 的截图清单漏了后来新增的两张。做成：逐条对照代码核实并修正，以"每条声明都能在代码里找到对应"作为完成标准。

**Blocked by:** None（可立即开始）

**Status:** ready-for-agent

- [ ] 架构文档中不存在的目录描述已删除或补建，二者取其一并说明
- [ ] 需求文档引用的命令与实际脚本名一致，或直接可执行
- [ ] 架构文档中的表名与数据库实际表名一致（全部表逐条核对，不只修已知那一处）
- [ ] 示例环境变量文件中的每一项都有代码实际读取，无效项移除或补上读取逻辑
- [ ] README 的截图清单与实际生成的截图文件一致
- [ ] README 的页面数量、测试用例数量与实际一致
- [ ] 逐条核对：文档中每一条可验证声明都能在代码或运行结果中找到对应证据
- [ ] 全量测试与类型检查保持绿色（若文档修正连带改动代码）
