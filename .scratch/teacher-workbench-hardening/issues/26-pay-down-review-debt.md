# 26: 清理复查指出的技术债

**What to build:** 两轮统一复查（Standards 轴）在已交付的代码里指出一批结构性债务。它们不影响功能正确性，但会让后续每一次相关改动都要在多处重复编辑，属于"现在不还、以后越滚越大"的那类。逐条清掉，行为保持不变。

**Blocked by:** None（可立即开始）

**Status:** done

- [x] **唯一硬性违规：院系范围逻辑重复四处**——已抽到 `apps/api/src/services/applicationScope.ts`，申请列表、待审批列表、审批动作、首页计数四处改为调用同一处定义
- [x] 登录限流的 `{ max, timeWindow }` 提取为具名类型 `LoginRateLimit`（定义在 `config/env.ts`，被接口与装配处共用）
- [x] "放弃未保存修改"由两套实现合并为一套：共享 hook 现在同时接受表单实例与判定函数，审批意见弹窗改用它，文案与复位行为不再分叉
- [x] 更新授课任务改为"校验所得即所用"：不再手工合并 14 个字段后丢弃校验结果，直接用校验产出的对象写入
- [x] 口令校验去掉类型断言：`promisify` + 强转改为显式 Promise 包装
- [x] 测试中重复的断言合并（密钥判定里两条完全相同的用例并作一条）
- [x] 全量测试与类型检查保持绿色，且端到端测试与改造前行为一致（纯重构，无功能改动）

## 验证方式

**不是"看着像收敛了"，是逐项机器核查：**

- 院系范围：全仓检索 `users.department`，**只剩 `applicationScope.ts` 两处**（取院系的投影 + 范围判定），其余业务模块零命中
- 未保存判断：全仓检索 `isFieldsTouched`，**只剩 hook 里一处**
- 直接断言路径参数的写法：**0 处**
- `promisify` 与随之而来的类型断言：**0 处**
- 更新授课任务里的条件展开：**0 处**
- 审批意见弹窗里手写的确认文案：**0 处**

**回归安全网：** typecheck exit 0；单元/集成/组件 **276 条全通过**（api 160，因合并重复用例较前减少 1）；端到端 **26/26 全通过、无 flaky**。因为这是纯重构，端到端全绿是"行为未变"的关键证据。

## 一处刻意的取舍

首页的待审批计数原先直接使用已在手上的院系值，抽出服务后改为调用同一处规则；为避免多一次查询，服务支持把已知院系传入。规则单点化与"零多余查询"两者兼得。
