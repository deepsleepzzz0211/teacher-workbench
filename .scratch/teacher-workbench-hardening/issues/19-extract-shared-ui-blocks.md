# 19: [expand] 抽取共享 UI 构建块

**What to build（第一步：并存，不替换）:** 六个页面各自复制了同样的统计卡行；八个弹窗复制了同样的表单外壳；五处复制了同样的删除确认；五个页面重复声明了同样的表格列类型。这些重复是后续无障碍、脏数据保护、主题化三条线改动量翻倍的根源——改一处要改六到八处。做成：新增共享构建块（统计卡行、表单弹窗、删除确认、表格列类型），**与现有实现并存，本轮不替换任何调用点**，CI 保持绿色。

**Blocked by:** 03 代码规范工具接入

**Status:** ready-for-agent

- [ ] 四个共享构建块就位，接口覆盖现有重复写法的全部用法（含自定义校验、异步提交、条件插槽）
- [ ] 现有页面一行未改，行为与外观完全不变
- [ ] 共享构建块自身有组件测试
- [ ] 共享构建块的样式与间距和现有页面一致，视觉无差异
- [ ] CI 保持绿色（这是 expand 阶段的硬性要求：新旧并存，随时可回退）
- [ ] 记录每个构建块将替代的重复位置清单，供下一张工单使用
